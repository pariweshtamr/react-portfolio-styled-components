import styled from 'styled-components'

export const TimelineStyled = styled.section`
    .small-title{
        padding-bottom: 2rem;
    }
    .timeline-content{
        border-left: 2px solid var(--border-color)
    }
`